# Property Rental Service API

REST API для сервиса аренды недвижимости.

**Автор:** Зарецкий Юрий  
**Группа:** БР2.2  
**Задание:** Домашняя работа №2

---

## 📋 Содержание

- [Формат API](#-формат-api)
- [Структура проекта](#-структура-проекта)
- [Запуск Swagger UI](#-запуск-swagger-ui)

---

## 🎯 Формат API

**Выбран: REST API**

---

## 📁 Структура проекта

```
hm2/
├── main.tsp                    # Главный файл TypeSpec
├── tspconfig.yaml              # Конфигурация компилятора
├── package.json                # npm зависимости
├── server.mjs                  # Сервер для Swagger UI
├── docker-compose.yml          # Docker конфигурация
├── Dockerfile                  # Сборка образа
├── README.md                   # Документация (этот файл)
│
├── models/                     # Модели данных
│   ├── error.tsp               # Модели ошибок
│   ├── param.tsp               # Параметры пагинации/сортировки
│   ├── auth.tsp                # Модели аутентификации
│   ├── user.tsp                # Пользователь
│   ├── city.tsp                # Город
│   ├── comfort.tsp             # Удобство
│   ├── property.tsp            # Объект недвижимости
│   ├── property-photo.tsp      # Фотография объекта
│   ├── rent-deal.tsp           # Сделка аренды
│   ├── conversation.tsp        # Переписка
│   ├── message.tsp             # Сообщение
│   └── review.tsp              # Отзыв
│
└── interfaces/                 # API эндпоинты
    ├── auth.tsp                # /auth/*
    ├── users.tsp               # /users/*
    ├── cities.tsp              # /cities/*
    ├── comforts.tsp            # /comforts/*
    ├── properties.tsp          # /properties/*
    ├── property-photos.tsp     # /properties/{id}/photos/*
    ├── rent-deals.tsp          # /rent-deals/*
    ├── conversations.tsp       # /conversations/*
    ├── messages.tsp            # /conversations/{id}/messages/*
    └── reviews.tsp             # /reviews/*
```

---

## 🚀 Запуск Swagger UI

### Вариант 1: Через Docker (рекомендуется для защиты)

```bash
# 1. Перейти в папку проекта
cd "БР2.2/Зарецкий Юрий/homeworks/hm2"

# 2. Собрать и запустить контейнер
docker-compose up --build

# 3. Открыть в браузере
# Swagger UI: http://localhost:8000/docs
# ReDoc:      http://localhost:8000/docs/redoc
```

### Вариант 2: Локальный запуск

```bash
# 1. Перейти в папку проекта
cd "БР2.2/Зарецкий Юрий/homeworks/hm2"

# 2. Установить зависимости
npm install

# 3. Скомпилировать TypeSpec в OpenAPI
npm run build

# 4. Запустить сервер документации
npm start

# 5. Открыть в браузере
# Swagger UI: http://localhost:8000/docs
# ReDoc:      http://localhost:8000/docs/redoc
# OpenAPI:    http://localhost:8000/docs/openapi.yaml
```
